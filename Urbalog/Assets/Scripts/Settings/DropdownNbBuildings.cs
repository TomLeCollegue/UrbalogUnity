using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class DropdownNbBuildings : MonoBehaviour
{
    public Dropdown _dropdownNbPerTurn;

    public List<int> numberList = new List<int>() { 0, 1, 2, 3, 4, 5 };

    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
